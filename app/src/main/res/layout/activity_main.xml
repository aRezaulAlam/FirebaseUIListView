<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.agroho.FirebaseUIListView.MainActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/fruitNameEditText"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="54dp"
        android:layout_toLeftOf="@+id/fruitSubmitButton"
        android:layout_toStartOf="@+id/fruitSubmitButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Submit Fruit Name"
        android:id="@+id/fruitSubmitButton"
        android:layout_alignBottom="@+id/fruitNameEditText"
        android:layout_alignRight="@+id/fruitListView"
        android:layout_alignEnd="@+id/fruitListView" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/fruitListView"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/fruitNameEditText" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/editTextSummary"
        android:layout_above="@+id/fruitSubmitButton"
        android:layout_alignRight="@+id/fruitSubmitButton"
        android:layout_alignEnd="@+id/fruitSubmitButton"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

</RelativeLayout>
